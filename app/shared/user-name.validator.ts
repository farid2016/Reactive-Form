// import { AbstractControl } from "@angular/forms";

//  export function frobiddenNameValidator(controll: AbstractControl): {[key: string]:any} | null{
//     const frobidden = /admin/.test(controll.value);
//     return frobidden ? {'frobiddenName': {value: controll.value}} : null;

// }